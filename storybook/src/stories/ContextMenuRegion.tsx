import React from "react";
import FlutterRenderer from "../components/FlutterRenderer";

interface ContextMenuRegionProps {
}

export const ContextMenuRegion = (props: ContextMenuRegionProps) => {
  return (
    <FlutterRenderer {...props} />
  );
};
